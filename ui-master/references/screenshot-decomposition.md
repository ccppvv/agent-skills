# 截图组件化拆解模板（强制前置）

在开始视觉还原前，先完成“组件库存检索 + 组件化拆解”，并等待用户确认。

## 0. 组件库存检索（common-components）

- 检索目录：`src/components/common-components/`
- 检索结论：
  - 可复用组件：<列表>
  - 可扩展组件：<列表>
  - 缺失组件（建议新增）：<列表>

## 1. 页面结构拆解

- 页面：<页面名>
- 主容器：<如 page/layout/content>
- 一级区块：<区块A/区块B/区块C>
- 二级区块：<按区块细分>

## 2. 组件清单（回传用户）

| 序号 | 组件名 | 层级路径 | 类型 | 布局关系 | 候选复用组件（common-components） | 决策（复用/扩展/新增） | 关键样式点 | 计划复用 Token |
|---|---|---|---|---|---|---|---|---|
| 1 | 页面标题区 | page > header | 信息组件 | 横向 | CommonHeader | 复用 | 标题字号、底部间距 | font-size / spacing |
| 2 | 筛选表单区 | page > filter-card | 表单组件 | 栅格/横向 | FilterPanel | 扩展 | 行高、控件高度、列间距 | control-height / gap |
| 3 | 内容卡片区 | page > content-card | 容器组件 | 纵向 | 无 | 新增 | padding、边框、圆角 | card-padding / radius |

## 3. 复用决策说明

- 复用：<为什么可以直接复用>
- 扩展：<扩展点及兼容策略>
- 新增：<新增理由、预期复用面（至少 2 处）>

## 4. 还原范围声明

- 本次覆盖：<组件列表>
- 暂不覆盖：<组件列表 + 原因>

## 5. 用户确认门禁

在用户明确回复“拆解正确/可以开始还原”之前，不进入 DESIGN tokens 应用与代码修改。

推荐确认话术：
- “我已完成 common-components 检索与截图组件化拆解，请确认上表的复用/扩展/新增决策。确认后我再开始应用 DESIGN tokens 做视觉还原。”
